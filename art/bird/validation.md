# Reference bird validation

- Built in Blender 5.2.1 LTS from the supplied bird drawing, preserved in `art/references/bird-design.png` and packed into the editable source. The same geometry is installed in the cast library and shared recipe.
- Visually reviewed the studio render, all eight directions, and production arena capture. Preserved the solid orange body and head, black eye dot, yellow beak, asymmetric white feather wings with golden eyes, and human feet. Refined feather coverage, round irises, foot lighting and toe anatomy. Increased opposing wing depth after checking head-on views.
- Final GLB: 30,145 triangles, 21,731 source vertices, one mesh, four materials, no texture images, 967,312 bytes. The paint exports unlit; feather, skin and eye surfaces stay shaded. Both GLB copies match, vertex colors survive import, and Godot generates LODs.
- Eight 192 x 192 RGBA sprites total 86,350 bytes, 62% smaller than the previous 230,126-byte bird set. All eight directions are distinct and nonempty, keep transparent margins, and have visible wing width. The orange fill remains exact, with opaque interiors in both the body and head. Sources, references and GLBs are excluded from the web package.
- Passed `bird_model_test`, `cast_integration_test`, `model_art_test`, `snake_model_test`, `owl_model_test`, `fox_model_test`, `cat_model_test`, `top_down_test`, `smoke_test`, `fun_systems_test`, `playability_test`, `keyboard_aim_test`, and `balance_test` with Godot 4.7.2. Rechecked bird integration and sprite framing after filling the body and head.
- The complete roster test spawns all ten characters and fourteen props through actual gameplay paths, verifies all 94 production sprite references and enemy introductions, and saves `art/all-models-in-game.png` when run graphically. The reference creatures, remaining enemies, bosses and props all use their authored models through the shared sprite renderer. Labels and staging are confined to the test.
- Verified the opening-wave bird introduction, 16-unit collision radius, one-seed health, weaving pursuit and an actual seed projectile kill. The production renderer preserves authored colors and provides room for the wings, feet and health bar.
- Created the release Web export and checked browser loading, Play interaction, opening-wave combat and the game-over screen, with no console errors. The native production capture stages real birds at several facings; combat behavior is covered by the regression test.
- The model is static, without an armature or animation clips. Movement, bounce, squash and combat effects remain procedural. Wing backs and depth are inferred from the single supplied reference.
- Replaced the overlapping beak pieces with one closed, rounded yellow bill and shortened the orange snout inside it. Checked the whole bill in the studio preview and all eight game facings, synchronized the cast library, and reran bird integration, the complete roster, sprite framing and the Web export. The beak no longer has the orange intrusion that made it appear broken.
- Corrected uneven wing lighting with a neutral environment and six balanced softboxes. Hid the presentation floor, which occluded the lower hemisphere on the tilted upright wing, and saved a rendered Cycles viewport so the editable source matches the preview. Visually checked the refreshed studio image, eight sprites and arena captures, then reran bird integration, the complete roster, sprite framing and the Web export. Both wings retain feather detail with no clipped white patches in the sampled studio wing regions.
